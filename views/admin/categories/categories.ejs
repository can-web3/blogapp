<div class="d-flex justify-content-between align-items-center gap-4 flex-wrap">
    <h1 class="h6 mb-0 text-uppercase"><%= title %></h1>
    <a href="<%= routes.adminCategoriesCreate %>" class="btn btn-success d-flex align-items-center">
        <i class="fas fa-plus"></i>
        Ekle
    </a>
</div>
<hr/>
<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="datatable table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th>Adı</th>
                        <th class="th-actions">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    <% categories.forEach(category => { %>
                        <tr>
                            <td><%= category.name %></td>
                            <td>
                                <a href="<%= routes.adminCategoriesEdit.replace(':id', category._id) %>" class="btn btn-sm btn-warning me-2">Düzenle</a>
                                <button 
                                    type="button" 
                                    class="btn-delete btn btn-sm btn-danger"
                                    data-route="<%= routes.adminCategoriesDelete.replace(':id', category._id) %>"
                                    data-name="<%= category.name %> kategorisini silmek istediğinize emin misiniz?"
                                >
                                    Kaldır
                                </button>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</div>